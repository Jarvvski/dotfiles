# Layouts / Multiplexing
enabled_layouts splits:split_axis=horizontal, stack

# Splits layout provides WezTerm-style pane management
# - cmd+t creates a vertical split (see 05-keys.conf)
# - cmd+shift+z toggles stack (zoom) layout
# - cmd+[/] navigates between panes
